-
    hosts: localhost
    name: 'Hello World!'
    tasks:
        - name: Display Password from Vault
          debug:
            msg: "{{ ansible_password }}"
        - name: Display Host from Vault
          debug:
            msg: "{{ ansible_testhost }}"
             
