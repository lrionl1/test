---
- name: Show credentials from vault
  hosts: all
  gather_facts: false
  tasks:
  
    - name: Display test host from Vault
      debug:
        msg: "{{ ansible_testhost }}"
    - name: Test connection to host
      ping:    
